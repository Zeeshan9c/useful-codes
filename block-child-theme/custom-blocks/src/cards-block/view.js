/**
 * view.js — front-end only script for card-block.
 *
 * This file is only loaded on the front end when the block is present on the page.
 * Use it for any interactive behaviour (e.g. click handlers, animations).
 */

// Example:
// document.querySelectorAll( '.card-block' ).forEach( ( card ) => {
//     card.addEventListener( 'click', () => { ... } );
// } );